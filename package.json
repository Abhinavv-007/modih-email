{
  "name": "modih-mail",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public --d1=DB --kv=RATE_LIMIT --port 8788",
    "deploy": "wrangler pages deploy public --project-name=modih-mail",
    "db:init": "wrangler d1 execute modih-mail-db --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.60.0"
  }
}
